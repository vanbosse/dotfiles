# Load rbenv
if which rbenv > /dev/null; then eval "$(rbenv init -)"; fi

# Load nvm
if brew --prefix nvm > /dev/null
    then
        export NVM_DIR="$HOME/.nvm"
        . "$(brew --prefix nvm)/nvm.sh"
fi
